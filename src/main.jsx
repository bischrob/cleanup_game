import React from 'react';
import ReactDOM from 'react-dom/client';
import CleanupGameTracker from './cleanup_game_tracker.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CleanupGameTracker />
  </React.StrictMode>
);
