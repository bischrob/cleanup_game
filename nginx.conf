server {
  listen 80;

  location /cleanup/ {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri $uri/ /cleanup/index.html;
  }

  # Optional: Redirect root to /cleanup/
  location = / {
    return 302 /cleanup/;
  }
}
